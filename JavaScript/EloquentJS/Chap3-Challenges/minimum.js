/**
 * @function minimum
 * @param {Number} numOne
 * @param {Number} numTwo
 * @description
 * Write a function similar to Math.min that takes
 * two numbers and return the smaller one.
 */
function minimum(numOne, numTwo) {
  return numOne < numTwo 
    ? numOne 
    : numTwo;
}